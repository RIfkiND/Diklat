import React from "react";

const Circle = (props) => {
  return (
    <img
      src="/images/ilustrasi/Circlee.svg"
      alt="Monitor illustration"
      className="w-full h-full"
      {...props}
    />
  );
};

export default Circle;
